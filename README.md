# gitdemo-trial
# Student Database Management System

A Python application that connects to a MySQL database to manage student records and marks using `mysql-connector-python` and `python-dotenv`.

## Features
- Connects securely to a local MySQL database.
- Uses environment variables (`.env`) to keep database credentials secure and out of version control.
- Performs database operations for managing student data and marks.

